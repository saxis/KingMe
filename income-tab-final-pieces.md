# FINAL PIECES TO ADD TO INCOME TAB

## 1. ADD THREE NEW MODALS (before the closing </View>, after the existing deduction modal around line 1073)

Add this AFTER your existing "ADD DEDUCTION MODAL" closes but BEFORE the final `</View>`:

```tsx

```
